//搜索图标样式
import styled from "styled-components";

const SearchIcon = styled.span`
  position: absolute;
  top: 0.8rem;
  left: 1rem;
  height: 2rem;
  width: 2rem;
  font-size: 1.5rem;
  font-family: cursive;
`;

export default SearchIcon;
